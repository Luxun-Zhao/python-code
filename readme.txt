this code could receive data from the serial and save as an text. At the same time, this script also could pilot on the screen.
the data get from the serial must follow next standard:

  0xaa 0xaa 0xf1 data

this is one data frame.

the code would run when .bat has been called. befor this, the ID of serial need to be assigned.
